const nomes = ["Leonardo", "Maria", "João"];
const removido = nomes.pop(); // Excluindo e retornando o último item do array da variável removido

// element.pop(); (exclui o último item do array)
// element.shift(); (exclui o primeiro item do array)

// Usando o shift vai deslocar os outros itens do array
// para a esquerda mudando o indice dos outros itens

console.log(nomes);
console.log(removido); // Retornando o item removido
