/* 

Operadores de comparação

>   maior que
>=  maior que ou igual a
<   menor que
<=  menor que ou igual a
==  igualdade (valor) // Não vamos usar
=== igualdade estrita (checa valor e tipo) // Usar esse
!=  diferente (valor) // Não vamos usar
!== difetente estrito (checa valor e tipo) // Usar esse

*/

const num1 = 10;
const num2 = "10";
const compara = num1 === num2;
console.log(compara);
